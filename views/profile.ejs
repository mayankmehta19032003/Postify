<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-between mb-5">
        <a href="#" class="text-4xl font-bold bg-gradient-to-r from-blue-500 to-pink-500 bg-clip-text text-transparent">
          Postify...
        </a>
        
        <a
          class="bg-red-600 rounded-md px-3 py-2 text-sm mb-5 inline-block"
          href="/logout"
          >Logout</a
        >
      </div>
      <h3 class="text-3xl">
        <span class="font-light">Hello,</span> <%= user.name %> 👋
      </h3>
      <h5 class="mb-2 mt-3">You can create a new post.</h5>
      <form action="/post" method="post">
        <textarea
          placeholder="What's on your mind?"
          name="content"
          class="block w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
        ></textarea>
        <input
        class="px-3 w-40 text-sm py-2 bg-blue-500 block rounded-md mt-2 hover:bg-blue-700 cursor-pointer"
        type="submit"
        value="Create new post"
      />
      
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your posts....</h3>
        <div class="postcontainer mt-5 flex gap-4 flex-wrap">
          <% user.posts.forEach(function(post){ %>
          <div
            class="post w-1/4 p-5 mb-3 border-2 rounded-md bg-zinc-800 border-zinc-800"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>

            <div class="btns flex gap-4 mt-5">
              <a class="text-blue-500 " href="/like/<%= post._id%>">
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"
                %></a
              >
              <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
              <a class="text-red-500 hover:text-red-700" href="/delete/<%= post._id%>">Delete</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
